[project]
name = "timestransfer"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "einops>=0.8.1",
    "numpy>=2.2.3",
    "statsmodels>=0.14.4",
    "torch>=2.6.0",
]

[tool.ruff]
line-length = 120
